import React, { useState } from 'react';

const [selectedOption, setSelectedOption] = useState('Custom');
const [showCustomInputs, setShowCustomInputs] = useState(true);

// ... rest of the component code ... 